## Note

To simplify evaluation, the repository includes the `.env` configuration and database connection details for a dedicated assessment database containing synthetic data only.

This allows the application to be run without any additional setup or configuration using one command only, as below:

```bash
docker compose up --build
```
